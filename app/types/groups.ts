export type UserLeague = {
  groupId: string
  name: string
  inviteCode: string
  createdAt: string
  role: string
  joinedAt: string
}

export type CreatedGroup = {
  id: string
  name: string
  invite_code: string
  created_at: string
  created_by: string
}
